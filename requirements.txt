# ── Google ADK & AI ───────────────────────────────────────────────────────────
google-adk>=1.0.0           # Google Agent Development Kit — core framework
google-genai>=0.8.0         # Raw Gemini SDK (used for OCR agent + structured outputs)

# ── MCP Server ────────────────────────────────────────────────────────────────
mcp>=1.0.0                  # Model Context Protocol base library
fastmcp>=0.1.0              # FastMCP: Rapid MCP server development framework

# ── Web Framework ─────────────────────────────────────────────────────────────
fastapi>=0.110.0            # API framework — async, production-ready
uvicorn[standard]>=0.29.0   # ASGI server for FastAPI

# ── Database ──────────────────────────────────────────────────────────────────
sqlalchemy>=2.0.0           # ORM for SQLite audit ledger

# ── Data Validation ───────────────────────────────────────────────────────────
pydantic>=2.0.0             # Strict JSON schema enforcement for agent outputs

# ── File Processing ───────────────────────────────────────────────────────────
pillow>=10.0.0              # Image file handling (PNG, JPG for OCR)
python-multipart>=0.0.9     # Multipart file upload support for FastAPI
pypdf>=5.0.0                # Local PDF text extraction (offline fallback)


python-dotenv>=1.0.0        # Loads GEMINI_API_KEY from .env file safely

# ── PDF Generation ────────────────────────────────────────────────────────────
fpdf2>=2.8.0                # Styled PDF compliance reports
