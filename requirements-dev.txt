# ── Development & Testing Dependencies ───────────────────────────────────────
# Install with: pip install -r requirements-dev.txt
# These are NOT needed in production — only for local development and CI/CD

pytest>=8.0.0               # Test runner
pytest-asyncio>=0.23.0      # Async test support for FastAPI endpoints  
httpx>=0.27.0               # Async HTTP client for FastAPI TestClient
pytest-cov>=5.0.0           # Code coverage reporting
ruff>=0.4.0                 # Fast Python linter (replaces flake8 + isort)
